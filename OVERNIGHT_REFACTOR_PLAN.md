# Overnight Refactor — Game Loop & Health Plan

**Branch:** `feature/general-fixes`
**Working tree:** clean
**Goal:** Comprehensive single-player polish + architectural cleanup that doesn't paint MP into a corner. User is asleep; safe defaults everywhere; build stays green at every phase.

---

## Context

The user opened with two asks:

1. **Gitignore `.claude/worktrees/` and delete the tracked files.** Concrete cleanup item.
2. **Wide health scan + overnight refactor.** Take full control of the gameloop, fix errors and inconsistencies, structure the work so multiplayer is viable later.

Three audits were run in parallel (gameplay loop, code quality, architecture/MP-readiness). They converged on the same picture:

- **No critical runtime crash bugs.** Build is healthy.
- **Real game-feel bugs exist** (cargo cell miscount across ports, dialogue-while-walking, fragile player spawn, dead harbour-dues screen, shipwright doesn't validate before charging).
- **Persistence layer is captain-only** — accepted contracts, in-flight cargo, ship position, player on-foot position, throttle/rudder state all reset on reload. This is the biggest single-player feel gap.
- **Fuel station exists as decoration** — model loads, no gameplay mechanic behind it.
- **Architecture has four overlapping "player state" abstractions** (`PlayerSession`, `GameState`, `LocalPlayerView`, `PlayerVessel`). The MP seam (`LocalPlayerView`) was added but everything bypasses it.
- **Two interaction surfaces** (`BridgeInteractable`, `CaptainsChair`) have ~250 lines of near-identical code.
- **Asset paths are scattered** across 20+ files (hat models, pallet models, port buildings).
- **3 duplicate `_load_json` implementations** (`ship_builder.gd`, `mesh_transformer.gd`, `model_assembler.gd`).
- **`.claude/worktrees/`** committed by accident from prior agent sessions.

The plan below is structured into phases. Each phase is **a single commit**, **verifiable in isolation**, and **leaves the build playable**. Order is chosen so early phases unblock later ones and the architectural unification (Phase B) lands before the features that depend on it (Phases C-F).

---

## Phase 0 — `.claude/worktrees/` cleanup

**Risk:** trivial.
**Why first:** the user asked for it explicitly; also a one-line `.gitignore` change so we don't recommit those files later.

**Files:**
- `.gitignore` — append `.claude/worktrees/` and any other regen-from-import noise patterns (e.g. specific `.uid` paths that get repeatedly resurfaced from `--import`)
- `.claude/worktrees/*` — `git rm --cached` the 4 tracked session dirs
- The four orphan `.uid` files that get regenerated by `--import` (`scripts/player/character_appearance.gd.uid`, `scripts/ui/character_creator_panel.gd.uid`, `scripts/ui/character_preview.gd.uid`, `scripts/ui/main_menu.gd.uid`) — confirm they're not needed; if so, add a targeted ignore so future `--import` runs don't dirty the tree.

**Verify:** `git status` clean after a fresh `godot --headless --import`.

---

## Phase 1 — Quick correctness fixes (small, surgical)

**Risk:** low. Each fix touches one file, one function.

These are all confirmed bugs from the audit. Bundling them into one commit because individually they're tiny.

### 1.1 Cargo cell miscount across ports

`scripts/npc/contract_npc.gd::_ship_cells_free()` (and any sibling that counts free cells) currently counts cells where `CargoDeckComponent.port_id.is_empty()`. A ship floating in open water (unmoored) reports all cells as free; a player can over-commit to two ports' worth of cargo.

**Fix:** filter on **the deck's `port_id` matching the contract's `origin_port_id`, OR the deck being moored at the same port as the contract NPC.** Get current mooring state from `MooringComponent` or `PortDock`.

Existing infra to reuse: `MooringComponent._sync_berth_with_dock()` already sets `port_id` on the deck when moored. So we can require `deck.port_id == this_npc.port_id` for cells to count as available.

### 1.2 NPC dialogue input suppression

`scripts/npc/npc_interactable.gd::_on_interact()` opens a dialog but doesn't stop the player from walking. The player can wander away mid-conversation.

**Fix:** on `open_ui()`, call `player.set_process_unhandled_input(false)`; on `close_ui()`, restore. Or — cleaner — set `Input.set_mouse_mode(VISIBLE)` and route the dialog's `process_mode` so it pauses gameplay. Either approach works; pick the lighter one (input-suppression flag on the player node).

Existing infra to reuse: `CaptainsChair._board()` already does `_player.set_process_unhandled_input(false)`. Mirror that pattern.

### 1.3 Player spawn safety

`scripts/world/world.gd::_spawn_player()` falls back to `Vector3(0, 0.5, 0)` if `PortPlot.get_spawn_position()` returns null. No collision validation.

**Fix:** after computing the spawn position, validate it with a tiny `PhysicsDirectSpaceState3D.intersect_point` query (player capsule, layer "world"). If it intersects, raise Y by `0.5` and retry up to 3 times. As ultimate fallback, snap to the home port's `PortDock.get_spawn_position()` even if `get_spawn_position()` returned something odd.

### 1.4 Wire `distance_sailed_m`

`PlayerSession.add_distance_sailed()` exists but is never called.

**Fix:** in `BoatBody._physics_process()` (or `BoatController`), integrate `linear_velocity.length() * delta` and emit to `PlayerSession.add_distance_sailed()` once per second (not per physics tick — debounce). Only when the boat is helmed by the player, to avoid counting AI ship motion.

Existing infra to reuse: `LocalPlayerView._helmed` already tracks the helmed boat. Subscribe to its `helm_changed` and integrate on the active boat.

### 1.5 Remove dead PAY_DUES screen

`scripts/npc/harbour_master_npc.gd::_Screen.PAY_DUES` exists and always returns "no dues."

**Fix:** delete the enum value, the screen handler, and the button that leads there. Trim ~30 lines.

### 1.6 Shipwright pre-charge validation

`scripts/npc/shipwright_npc.gd::_try_pay_for_commission()` charges the player marks *before* checking the hull JSON exists. If `HullStations.from_hull_json(null, ...)` crashes, the player is charged and gets nothing.

**Fix:** validate `FileAccess.file_exists(hull_path) and ShipBuilder._load_json(hull_path).has("parts")` before deducting marks. Refund-by-not-charging is the right pattern.

### 1.7 Mooring split-berth UI feedback

`scripts/ship/mooring_component.gd::_mooring_splits_berths()` rejects mooring with no UI feedback.

**Fix:** on rejection, emit a signal like `mooring_rejected(reason: String)`. The ShipHud can render a short flash message ("Lines on different berths — release and retry").

Existing infra to reuse: `ShipHud` already draws status badges (lights, wind). A flash overlay is a small addition.

**Verify Phase 1:** boot the world. Walk into NPC and confirm you can't walk away mid-dialogue. Accept a contract at home, sail to a second port without delivering, try to accept a contract — should reject with proper free-cell count. Sail near a long dock and tie lines across two berths; HUD shows rejection.

---

## Phase 2 — DRY pass: consolidate duplication

**Risk:** low (refactor with green build at each step).

### 2.1 Centralize `_load_json`

Three identical implementations: `ship_builder.gd:625`, `mesh_transformer.gd:239`, `model_assembler.gd:310`.

**Fix:** add `scripts/core/json_util.gd` with `static func load_json(path) -> Dictionary`. Remove the three duplicates; update callers (`shipwright_npc.gd`, `shipwright_preview.gd`, `starter_vessel.gd`) to use the new util.

### 2.2 Centralize asset paths

Hat models, pallet models, port buildings repeated across many files. Add `scripts/core/asset_paths.gd` with named constants.

Files that benefit: `character_appearance.gd`, `contract_npc.gd`, `harbour_master_npc.gd`, `npc_base.gd`, `pallet_node.gd:200-207`, `port_facilities.gd:13-20`, `ship_builder.gd`, `boat_body.gd`, `starter_vessel.gd`.

Don't change behaviour — purely a rename / extract-const refactor.

### 2.3 Extract shared boarding-interactable base

`BridgeInteractable` and `CaptainsChair` share `_update_prompt()`, `_boarding_player()`, `_nearest_player_in_range()`, `Camera3D` lookup. Extract a shared base `BoardingInteractable` (or mixin via composition) in `scripts/ship/boarding_interactable.gd`. Both subclasses keep their domain-specific board/exit logic but inherit the polling + prompt code.

**Verify Phase 2:** parse-check; ensure the world still boots; players can still board ships and talk to NPCs.

---

## Phase 3 — `LocalPlayerView` as the single source of truth

**Risk:** medium. Touches a lot of UI but each migration is small.

The MP seam exists; nothing uses it. Make it the canonical lookup for "what is the player doing" and fold `PlayerVessel` into it as an internal helper.

### 3.1 Fold `PlayerVessel` group-tagging into `LocalPlayerView`

`PlayerVessel` (RefCounted static) tags ships with "player_boat" and despawns old hulls. Move these into `LocalPlayerView` methods:

```
LocalPlayerView.mark_active_ship(ship)
LocalPlayerView.despawn_other_ships(except=null)
LocalPlayerView.unregister_ship_from_docks(ship)
```

Old `PlayerVessel` becomes a thin forwarding shim for one commit (back-compat); next commit removes it.

### 3.2 Migrate `GameState` to read through `LocalPlayerView`

`GameState._wire_player_session()` and `_wire_contract_registry()` currently grab `/root/PlayerSession` and `/root/ContractRegistry` directly. Replace with `LocalPlayerView.session()` / `LocalPlayerView.registry()` accessors.

Keep `GameState` — it remains the read model for sub-state signals. Internal source switches.

### 3.3 Migrate `ShipHud` and `WalkingHud` and NPC interactables

Every `get_node_or_null("/root/PlayerSession")` in UI and NPC code becomes `get_node_or_null("/root/LocalPlayerView")`. Strict rule: **no UI code directly accesses PlayerSession or ContractRegistry from this commit onward.**

`WalkingHud` already partially migrated; finish it.

Files: `scripts/ui/ship_hud.gd`, `scripts/ui/walking_hud.gd`, `scripts/ui/main_menu.gd`, `scripts/ui/game_menu.gd`, `scripts/ui/debug_draw.gd`, `scripts/npc/harbour_master_npc.gd`, `scripts/npc/shipwright_npc.gd`, `scripts/npc/contract_npc.gd`.

### 3.4 Drop `PlayerVessel.gd`

Remove the shim. All callers now go through `LocalPlayerView`.

**Verify Phase 3:** boot world, commission a ship, helm it, sail, dock, talk to all three NPCs. HUDs update correctly. Marks display, contracts accept, ship commissions all still work — but every read passes through `LocalPlayerView`.

---

## Phase 4 — Persistence completeness

**Risk:** medium. New data fields in `PlayerData` need version-bump handling.

### 4.1 Bump save format version + migration

`PlayerSaveStore` envelope is `{ version: 1, player: {...} }`. Bump to version 2. Add a `migrate_v1_to_v2(envelope)` step that fills new fields with safe defaults so existing saves keep loading.

### 4.2 Persist accepted contracts

`PlayerData.accepted_contracts: Array[Dictionary]` — each entry `{contract_id, taken_count, delivered_count}`.

On save: snapshot `ContractRegistry.get_accepted_contracts()` into this array.

On load: `ContractRegistry.replay_accepted(records)` re-calls `accept_contract()` per record with the saved counts. This replays the *intent* without re-spawning pallets — pallets are restored separately (4.4).

### 4.3 Persist ship runtime state

`PlayerData.active_vessel` currently has `{uid, hull_id, display, template_path}`. Add:

```
ship_position:    Vector3
ship_rotation_y:  float  (yaw only, full quat is overkill for surface boats)
throttle_stage:   int
rudder_angle:     float
fuel_pct:         float  (placeholder, filled in Phase 5)
```

Save: on quit, helm-exit, and periodic auto-save (every 60s of wall-clock, debounced via `_request_save()`).

Load: after harbour master spawns the ship, restore position/rotation/throttle/rudder before the player boards.

### 4.4 Persist cargo manifest

`PlayerData.active_vessel.cargo: Array` of `{deck_name, cell_idx, contract_id, units}`.

Save: walk every `CargoDeckComponent` under the player's ship, serialize each pallet's contract reference + position.

Load: after spawn, walk the saved cargo records and re-add pallets to the corresponding decks. Uses existing `CargoDeckComponent.add_pallet()` path.

### 4.5 Persist on-foot player position

`PlayerData.player_position: Vector3`, `player_port_id: String`.

Save: whenever player is on foot (not helming), record position every 60s.

Load: if `player_port_id` matches an existing port, spawn the player at the saved position; if it doesn't, fall back to home port spawn.

**Verify Phase 4:** start new game, accept a contract, pick up cargo, sail halfway to the destination, quit. Relaunch — contract should still be accepted, cargo still on board, ship at the saved position with the saved throttle setting. If the player was on foot in a port, they wake up where they left off.

---

## Phase 5 — Fuel system MVP

**Risk:** medium. Touches several systems but mechanic is well-scoped.

### 5.1 Fuel tank on `BoatBody`

Add `@export var fuel_capacity_l: float` and `var fuel_l: float`. Capacity scales with hull (e.g. `displacement_kg * 0.01`). Filled to capacity on commission.

Methods: `consume_fuel(litres)`, `add_fuel(litres)`, `get_fuel_pct() -> float`.

### 5.2 Consumption in `PropulsionComponent`

In `_physics_process`, integrate `|throttle| * burn_rate_l_per_s * delta` and deduct from the parent BoatBody's `fuel_l`. Cap throttle at 0 if tank is dry (engine stalls). Burn rate per-ship from a hull-data field (added to hull JSON, with a sensible default if missing).

### 5.3 Refuel interaction at the pump

`FuelStation` currently just draws a model. Add an interactable mesh near `get_pump_position()` (use a small `Area3D` + `BoardingInteractable`-style prompt). On interact while standing next to it and a moored player ship is in range, transfer fuel from infinite station tank to the ship at a cost in marks (e.g. `0.1 marks / litre`). Use `LocalPlayerView.spend_marks()`.

### 5.4 Fuel gauge in `ShipHud`

Add a vertical fuel bar next to the throttle telegraph. Colour-coded: green > 30%, amber 10-30%, red < 10%. Flash on dry tank.

### 5.5 Out-of-fuel state

When `fuel_l == 0`, force throttle to 0 (`PropulsionComponent.max_thrust * 0` effective). HUD shows "DRY TANK — DRIFTING". Player still has rudder authority for current/momentum-driven steering but no thrust.

**Verify Phase 5:** start a new game, commission a ship at the shipwright, board it. Throttle up, sail until the fuel runs out (or burn-rate-cheat for testing), confirm propulsion stops. Sail/drift back to a port, dock, walk to the fuel pump, refuel, return to helm — fuel restored, propulsion resumes.

---

## Phase 6 — Cargo flow visibility + contract restock

**Risk:** low-medium. Mostly UI work + a timer.

### 6.1 Cargo journal UI

Add a small overlay to `ShipHud` that shows accepted contracts with progress bars:

```
COFFEE BEANS  Haugsvik → Bremsund      3 / 5 delivered  ●●●○○
TIMBER         Haugsvik → Solberg     0 / 8 in transit  ○○○○○○○○
```

Reuse `UiBuilder.key_value_row` for visual consistency.

Toggle visible/hidden via a hotkey (J for journal, configurable in input map).

### 6.2 Confirm `deliver_pallet()` invocation path

Audit found this code path "unclear." Trace from `CargoDeckComponent.deliver_pallet` upward to find where it's actually called. If it's not called anywhere — wire it: when a pallet enters a destination port's apron `CargoDeckComponent`, call `deliver_pallet()` automatically, or add an explicit "Drop here" interaction.

### 6.3 Contract restock via `WorldClock`

`ContractRegistry._export_stock` depletes but never replenishes. Add a periodic restock tied to `WorldClock`:

```
ContractRegistry._on_game_hour_tick():
  for port in _ports:
    for commodity in port.exports:
      stock[port][commodity] = min(max_stock, stock[port][commodity] + restock_rate_per_h)
```

Hook into `WorldClock.game_hour_changed` (or add that signal if missing).

**Verify Phase 6:** accept multiple contracts, pick up cargo, deliver at destination. Journal shows live progress. Wait some game-hours (or scrub time with debug) and confirm port stock has replenished.

---

## Phase 7 — Abandon-ship + small UX polish

**Risk:** low.

### 7.1 Abandon ship mechanic

Add a captain's chair option or `BoatController` key (`Esc` while helming → "Exit helm" already exists, so a separate `Shift+Esc` → "Abandon vessel"). On confirm:

1. Forfeit all cargo on board (existing `LocalPlayerView.despawn_other_ships` already does this — call it directly).
2. Despawn ship.
3. Respawn player at the home port.
4. Clear `active_vessel`; harbour master will offer the starter vessel again.

Confirms via dialog ("Abandon vessel? This forfeits all cargo and your current ship.").

### 7.2 Water rescue robustness

`Player._process` already teleports to `_last_safe_position` when below `abyss_reset_y`. Two improvements:

- Update `_last_safe_position` whenever the player crosses a `PortDock`'s `port_id` zone, not just `is_on_floor()`. This catches the "ran off the end of a long dock" case.
- Brief HUD flash when water rescue fires ("Pulled to shore").

### 7.3 Per-port spawn anchor metadata

Right now home port spawns the player at `PortDock.get_spawn_position()`. For consistency when loading saves with `player_port_id` (Phase 4.5), add `PortDock.get_safe_spawn_position()` that returns a guaranteed-safe spot (above the deck, inside the bounding box of the port plot, with collision validation).

**Verify Phase 7:** accept a contract, sail somewhere, hit Shift+Esc — confirm abandonment dialog, ship despawns, player back at home port, cargo gone, contract reset to available. Walk off the end of the dock — water rescue catches you and HUD flashes.

---

## Phase 7.5 — Character creation polish

**Risk:** low.

Character creator exists (`scripts/ui/character_creator_panel.gd` + `character_preview.gd`, NpcBase-based preview on the main menu) but the audit flagged the `_on_name_changed(_text: String)` callback as `pass` — name input doesn't propagate. The full round-trip from creator → PlayerData → in-game player model needs verifying end-to-end.

**Files:** `scripts/ui/character_creator_panel.gd`, `scripts/ui/character_preview.gd`, `scripts/ui/main_menu.gd`, `scripts/player/character_appearance.gd`, `scripts/player/player_data.gd`.

**Changes:**
- Wire `_on_name_changed` to update an internal `_pending_name` and pass it through to PlayerSession on "Confirm".
- Ensure `PlayerData.appearance` (skin/clothing/trousers colours, hat choice) is fully serialised through `to_dict()`/`from_dict()`.
- After main menu confirm, the in-game `Player` mesh hierarchy uses the same NpcBase-style rig with the chosen appearance — same code path the creator preview used.
- Add a "Randomize" button.
- Persistence: appearance round-trips through PlayerData v2 (already covered by Phase 4's schema bump — verify dict layout).

**Verify:** new captain → custom name + colours + hat → confirm → enter world. Walk to a reflective surface (or inspect via debug) — model matches the creator preview. Quit and reload; appearance persists.

---

## Phase 7.6 — Third-person camera (MP-critical)

**Risk:** medium. Camera changes are user-felt; needs careful default tuning.

Current `scripts/player/player.gd` is first-person mouse-look only. Multiplayer needs you to *see your own character* (and remote players see you in third-person), so introduce a switchable follow cam now.

**Files:** `scripts/player/player.gd`, new `scripts/player/player_camera.gd`.

**Changes:**

1. Extract camera management into a new `PlayerCamera` component that owns:
   - **First-person**: attached to the head, mouse-look applies yaw to body + pitch to head.
   - **Third-person**: orbit camera ~3–5 m back, ~1.6 m up, mouse-look orbits. Collision recovery: raycast from player to ideal cam pos; pull in if obstructed, lerp back out on clear.

2. Hotkey **V** toggles modes. Default: third-person on foot, first-person at helm (helm camera is `BoatCamera`, untouched).

3. In third-person, `_visual_root` (player mesh) becomes visible. In first-person, hide torso + head meshes (legs still visible looking down for foot-grounding feel).

4. Route the camera target through `LocalPlayerView.get_local_player_node()` so future remote players are renderable without confusion.

5. Smoothing: position lerp ~0.15 s, rotation lerp ~0.05 s. Avoid foot-jitter on uneven terrain (sample mean foot height over short lookback).

**Verify:** spawn at home port, press V → swap views. Walk near a building → camera doesn't clip through geometry. Helm a ship → switches to BoatCamera. Exit helm → returns to last-used on-foot mode.

**MP payoff:** when MP lands, other players' bodies and boats are visible because the camera no longer assumes "I'm inside my own head". Spectator / follow-other-player modes are a one-line trigger from here.

---

## Phase 8 — Architecture documentation refresh

**Risk:** trivial.

Update `AGENTS.md`, `ARCHITECTURE.md`, `SYSTEMS_AND_STRUCTURE.md` to reflect:

- `LocalPlayerView` is the canonical player-local-state seam; nothing should bypass it.
- `PlayerVessel` is gone; its responsibilities moved into `LocalPlayerView`.
- New persistence fields in `PlayerData` (post-Phase 4).
- Fuel system architecture (post-Phase 5).
- The "boarding interactable" base class.

The docs are the spec — keeping them current is what makes the next agent session not have to re-discover all of this.

Also add a short `SAVE_FORMAT.md` describing the envelope version 2 layout and migration path.

---

## Verification — end-to-end smoke after every phase

A single Godot headless smoke test that exercises the full critical path:

```
godot --headless --import          # refresh class cache
godot --headless --check-only --quit | grep -E "SCRIPT ERROR|Parse Error"
```

Plus phase-specific scripts as described per phase. Goal: build stays green at every commit boundary.

Final integration smoke: boot the world, board a starter vessel, accept a contract, sail to destination, deliver, refuel, save & restart, confirm everything resumes.

---

## Phase 9 — Settings menu

**Risk:** low-medium. Pure UI + project settings application.

No settings UI exists today. Required for any kind of public release. Reuses `UiBuilder` for visual consistency.

**Files:** new `scripts/ui/settings_menu.gd`, new `scenes/ui/settings_menu.tscn`, modify `scripts/ui/main_menu.gd` (add Settings button) and `scripts/ui/game_menu.gd` (add Settings button to pause).

**Changes:**

- **Graphics tab**: resolution dropdown, window mode (windowed/borderless/fullscreen), MSAA, VSync, FFT water resolution preset (low/med/high — maps to 256/512/1024 in `fft_water_system.gd`), volumetric fog toggle.
- **Audio tab**: master / SFX / ambient / UI sliders mapped to `AudioServer.set_bus_volume_db`.
- **Controls tab**: rebindable keys for movement, interact, helm controls, view toggle. Stored in `InputMap` overrides.
- **Persistence**: settings save to `user://settings.cfg` via `ConfigFile`; load at boot, applied before world spawn.

**Verify:** open from main menu, tweak each setting, restart game — settings persist and apply.

---

## Phase 10 — Pause-on-focus-loss + autosave heartbeat

**Risk:** trivial. Two small additions to existing autoloads.

### 10.1 Pause on focus loss

`GameMenu` already handles ESC pause. Add a `NOTIFICATION_APPLICATION_FOCUS_OUT` handler that triggers the same pause path. Resume requires the user to press ESC or click — never auto-resumes on focus-back, to avoid surprise input.

**Files:** `scripts/ui/game_menu.gd`.

### 10.2 Autosave heartbeat

Phase 4 saves on quit + event triggers. Add a 60-second wall-clock timer that calls `PlayerSession.save_now()` regardless. Insurance against crashes / power loss.

**Files:** `scripts/player/player_session.gd`.

**Verify:** play for 90 s, kill the process from task manager, relaunch — last 60 s of progress is preserved.

---

## Phase 11 — First-time tutorial chain

**Risk:** low. Pure UX flow.

A new captain spawns with no idea what to do. The audit found the gameplay loop *is* discoverable, but only if you know to look at the harbour master first. Add a subtle hint chain.

**Files:** new `scripts/ui/tutorial_hint.gd`, modify `scripts/state/local_player_view.gd` (tracks "first-time" state).

**Mechanism:** a single line of text floats at the bottom-centre of the screen with a soft fade. Sequence:

1. **On first spawn**, after the player has had control for 3 s: `"→ Speak to the Harbour Master to take a vessel."`
2. **After harbour master interaction**, on next on-foot moment: `"→ Visit the Shipwright to commission a custom hull, or skip and use your starter Coastal Trader."`
3. **First time aboard a ship**, after helm activation: `"W/S throttle · A/D rudder · Q/E bow thruster · ESC to leave helm"`
4. **First time accepting a contract**: `"→ Cargo is staged on the dock. Use the crane to load it onto your ship."`
5. **First time delivering**: `"→ Dock at the destination port, then unload the cargo to complete delivery."`
6. **After first delivery**: hints disable for that captain.

Stored as boolean flags on `PlayerData` (`tutorial_step_1_done`, etc., or one bitmask field). New captain = all false; existing save = all true (skip tutorial for returning players).

**Verify:** new captain → hints appear in order. Existing captain → no hints.

---

## Phase 12 — Hull damage + loading screen + fuel range + audio polish

**Risk:** mixed. Four small independent additions bundled into one phase since each is self-contained.

### 12.1 Hull damage from collision impact

`ShipData.hull_health` (0.0–1.0) exists but is never modified. Wire it:

- `BoatBody._integrate_forces()` or a collision contact monitor: when `_body.get_contact_count() > 0`, compute impact magnitude as `linear_velocity.length() * collision_mass`. If above a threshold (e.g. impact > 5 m/s · ship_mass), subtract from hull_health.
- At hull_health < 0.5, mild HUD warning; < 0.2, red "HULL CRITICAL" flash.
- Repair interaction at the harbour master: "Patch hull (ℳN)" where N scales with damage.

**Files:** `scripts/ship/boat_body.gd`, `scripts/ship/ship_data.gd` (signals), `scripts/ui/ship_hud.gd` (gauge), `scripts/npc/harbour_master_npc.gd` (repair option).

### 12.2 Loading screen with Telemetry log

Currently world spawn is silent for 1–2 s during port generation + SDF bake. Show a "Building world..." panel that consumes `Telemetry.load_events` and lists them as they complete:

```
✓ Building islands           (842 ms)
✓ Baking shelter             (203 ms)
✓ Generating ports           (157 ms)
✓ Spawning home port         (89 ms)
…
```

**Files:** new `scripts/ui/loading_screen.gd`, modify `scripts/world/world.gd` to show/hide it.

### 12.3 Fuel range circle on the map

Once Phase 5 lands, draw a green ring on `MapOverlay` representing the player's current fuel range (`fuel_l / burn_rate * cruise_speed`). Updates live as fuel drops.

**Files:** `scripts/ui/map_overlay.gd`.

### 12.4 Audio cues

Three short audio additions (re-use existing AudioStreamPlayer infrastructure in `boat_audio_system.gd`):

- **Splash on wave slam**: trigger when bow vertical velocity > 4 m/s relative to wave surface. Uses any existing wave/splash sample under `resources/audio/` or a placeholder white-noise burst.
- **Dock-bump thud**: triggered on collision contact between BoatBody and a dock collider (use a collision layer mask).
- **Low-fuel cough loop**: starts at fuel < 10 %, stops at refuel.

**Files:** `scripts/ship/boat_audio_system.gd`, `scripts/ship/boat_body.gd` (collision hooks).

**Verify:** ram a dock with throttle 100% → hull damage drops, thud audio plays. Sail through a heavy storm → splashes on bow slams. Run low on fuel → cough loop. Open map → ring shows where you can reach before drying out.

---

## What's deliberately out of scope

- **Multiplayer transport.** The user wants MP later but this plan is single-player polish.
- **Combat / threats / pirates.** No combat exists; not adding any.
- **Death of the player character.** Water rescue is the existing mechanic; we improve it but don't add a death state.
- **Visual asset upgrades.** The shipping art style (hand-modeled JSONs, vertex colours, no textures) is intentional; not touching it.
- **Map overlay further split.** Already shipped in PR #12; current 652-line state is fine.

---

## Full phase list (execution order)

| # | Phase | Risk | Primary value |
|---|---|---|---|
| 0 | Worktrees + gitignore + plan visible in repo | trivial | ✅ shipped |
| 1 | Quick correctness fixes (7 small bugs) | low | Real bugs gone |
| 2 | DRY pass (json util, asset paths, boarding base) | low | Cleaner codebase |
| 3 | LocalPlayerView as single source of truth | medium | MP seam alive |
| 4 | Persistence completeness (contracts, ship pose, cargo, on-foot) | medium | Quit/resume works |
| 5 | Fuel system MVP | medium | Actual mechanic |
| 6 | Cargo journal + contract restock | low-med | Loop visible |
| 7 | Abandon-ship + UX polish | low | Stuck-player exit |
| 7.5 | Character creation polish | low | Name input wired, appearance round-trips |
| 7.6 | Third-person camera (V toggle) | medium | MP-critical |
| 8 | Architecture docs refresh | trivial | Lock in decisions |
| 9 | Settings menu (graphics / audio / controls) | low-med | Ship-ready UI |
| 10 | Pause-on-focus-loss + autosave heartbeat | trivial | Crash insurance |
| 11 | First-time tutorial hint chain | low | New player onboarding |
| 12 | Hull damage + loading screen + fuel-range ring + audio cues | mixed | Polish bundle |

---

## Order rationale

- **0–2** are independent housekeeping; build stays playable throughout.
- **3** lands the seam *before* persistence (4) so saves go through the right interface from day one.
- **4** is the biggest single-player feel improvement — quitting and resuming actually works.
- **5** is the biggest missing mechanic — fuel gives the world stakes.
- **6** makes the gameplay loop legible and self-sustaining.
- **7 / 7.5 / 7.6** are polish + the camera lift; 7.6 specifically prepares for MP without doing the MP transport work.
- **8** locks in the architectural choices in docs.
- **9** is shipping-grade UI scaffolding.
- **10** is one commit's worth of crash-resilience polish.
- **11** is the missing onboarding — once everything else works, players need to be told how.
- **12** is the polish bundle that uses everything below (hull damage uses helm cam, loading screen uses Telemetry, fuel-range ring uses Phase 5, audio uses Phase 5).

Each phase ships as its own commit. If anything regresses, the bad phase is identifiable and revertable in isolation.

---

## Total estimated work

| Phases | Hours |
|---|---|
| 0–2 | 3–4 |
| 3 | 3 |
| 4 | 4–5 |
| 5 | 3–4 |
| 6–7 | 2–3 |
| 7.5 + 7.6 | 3 |
| 8 | 1 |
| 9 | 3 |
| 10 | 0.5 |
| 11 | 1.5 |
| 12 | 3 |

Roughly **27–30 hours** of focused work. Realistic for "overnight" only if the user's bar is "wake up to several of these landed". I'll execute top-down — phase 0 → phase 12 — and commit each one independently. Anything past whatever shipped is opt-in for the next session.
